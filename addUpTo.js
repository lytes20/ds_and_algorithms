// function addUpTo(n) {
//   let total = 0;
//   for (let i = 0; i <= n; i++) {
//     total = total + i;
//   }
//   return total;
// }

// function addUpTo(n) {
//   return (n * (n + 1)) / 2;
// }

// console.log(addUpTo(6));

/**
 * What does Better code mean
 * Faster
 * better memory intensive
 * Readability
 */

dns.resolve(ip, callback)